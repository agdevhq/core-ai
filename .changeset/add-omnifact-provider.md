---
'@core-ai/openai': minor
'@core-ai/omnifact': minor
---

Add `@core-ai/omnifact` provider for the Omnifact API Gateway with a default production base URL. Export `createOpenAICompatChatModel` from `@core-ai/openai/compat` and allow a custom provider id in the compat chat layer.
