---
'@core-ai/core-ai': minor
---

Add base64 `AudioPart` user content and validate audio through the existing directional input modality capabilities. Code that exhaustively handles `UserContentPart` must add an `'audio'` branch.
