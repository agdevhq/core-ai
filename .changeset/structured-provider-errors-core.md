---
'@core-ai/core-ai': minor
---

Add classified `ProviderError` codes and subclasses (`ContextLengthExceededError`, `RateLimitError`, `ModelOverloadedError`, `ServiceUnavailableError`) with an `isRetryable` signal.
