---
'@core-ai/core-ai': minor
---

Add a reusable model capabilities registry with normalized model ID lookup, a symbol-keyed fallback for unknown models, and explicit reasoning modes, sampling constraints, and supported tool choices.
