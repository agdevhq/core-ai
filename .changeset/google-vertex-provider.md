---
'@core-ai/google': minor
'@core-ai/google-vertex': minor
---

Add `@core-ai/google-vertex` for Gemini, embedding, and Imagen models on Google Vertex AI. Shares model behavior with `@core-ai/google` via `createGoogleProvider`. Authenticates with Application Default Credentials or an explicit service account key.
