---
'@core-ai/openai': minor
---

Add native reasoning round trips, JSON object structured output, request preparation, and support for overriding model capabilities through a shared registry. The structured output mode is now named `json-schema` instead of `native`.
