---
'@core-ai/xai': minor
---

Add `@core-ai/xai`, a provider for xAI Grok models. Uses the Responses API by default with stateless encrypted-reasoning round trips, and exposes the legacy Chat Completions API under `.chat`.
