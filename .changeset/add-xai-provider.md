---
'@core-ai/xai': minor
---

Add `@core-ai/xai` provider for xAI Grok API with native `reasoning_content` support, configurable `reasoning_effort` for `grok-4.3`, reasoning model parameter validation, and JSON Mode structured output via `generateObject` / `streamObject`.
